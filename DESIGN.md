# Lucking 界面设计约定

本文档记录当前前端已经落地的界面规则，范围为全局样式、`AppLayout`、`AppSidebar`、`AppTopbar` 与 J金股驾驶舱。它是实现现状的整理，不扩展尚未实现的设计能力。

## 1. “月石鎏金”视觉基调

界面以低疲劳的冷蓝灰作为大面积背景和结构色，以宝石蓝表达主要操作与信息层级，以少量香槟金强调研究优先级和当前导航。成功、警告、危险色只用于各自语义，不作为主题色；状态同时提供文字、图标或结构信息，不只依赖颜色。

### 1.1 亮色主题 token

| Token | 当前值 | 已用语义 |
| --- | --- | --- |
| `--lk-app-bg` | `#e9eef6` | 应用背景 |
| `--lk-sidebar-bg` | `#dce4f0` | 侧栏背景 |
| `--lk-surface` | `#f3f6fa` | 主表面、卡片、控件 |
| `--lk-surface-soft` | `#e5ebf3` | 次级表面、筛选栏、表头 |
| `--lk-surface-hover` | `#d7e0ec` | 悬停反馈 |
| `--lk-border` | `#bcc8d8` | 分隔线和控件边界 |
| `--lk-text` | `#25344d` | 主文字 |
| `--lk-text-secondary` | `#526178` | 辅助说明 |
| `--lk-text-muted` | `#526178` | 弱化标签 |
| `--lk-primary` | `#3f5f9a` | 主要操作、柱形和强调 |
| `--lk-primary-hover` | `#324e82` | 主要色的悬停态预留值 |
| `--lk-primary-contrast` | `#eef3fa` | 主要色上的文字 |
| `--lk-accent` | `#91a7c6` | 冷蓝辅助强调 |
| `--lk-fortune` | `#91670e` | 香槟金语义：品牌标记、当前导航、评分与等级 |
| `--lk-selection` | `#d9e3f2` | 选中态、状态标签底色 |
| `--lk-focus` | `#718cc0` | 键盘焦点环 |
| `--lk-danger` | `#ac5367` | 危险语义 |
| `--lk-success` | `#397b68` | 成功或工作台在线语义 |
| `--lk-warning` | `#9b6f2e` | 延迟、部分覆盖等警告语义 |

### 1.2 暗色主题 token

暗色主题由根节点 `.app-dark` 覆盖同名 token：应用背景 `#161d2b`、侧栏 `#1c2638`、主表面 `#232e42`、次级表面 `#2c3850`、悬停面 `#35445f`、边界 `#465673`；主文字为 `#dee6f1`，辅助与弱化文字均为 `#a4b0c4`；主要色为 `#a8c3f2`，主要色悬停为 `#8aaae0`，对比底色为 `#18253a`；香槟金为 `#f2c96b`，选中底色为 `#30405c`，焦点色为 `#9eb7e0`。危险、成功、警告分别使用 `#db8293`、`#65b59b`、`#d7b064`。

PrimeVue 的主色阶采用 Aura 预设并映射到同一套宝石蓝色系，从 `#eef3fa`（50）递进到 `#18253a`（950）。

## 2. 排版、间距与形状

- 全局字体栈为 `Inter, "Noto Sans SC", "Microsoft YaHei", system-ui, sans-serif`，兼顾数字、简体中文与系统回退。
- 页面通用标题使用 `clamp(1.7rem, 3vw, 2.4rem)`；J金股主标题使用更强的 `clamp(2rem, 4vw, 3.3rem)`，均配合负字距形成紧凑的数据产品标题。
- J金股模块标题为 `1rem`；辅助标签和说明主要位于 `0.68rem` 至 `0.8rem`；总览核心数字为 `1.72rem`，并使用等宽数字特性保持跳动稳定。
- 页面纵向堆叠间距以 `16px` 为主；驾驶舱主次网格间距为 `12px`；控件内部和列表项目常用 `8px`、`10px`、`12px`、`14px`、`18px`、`20px`、`24px` 的现有间距档位。
- 全局壳层在宽屏使用 `16px 24px 32px` 外边距与 `20px` 栏间距；紧凑布局改为 `10px 12px 24px` 与 `10px` 栏间距；窄屏为 `8px 10px 24px`。
- 通用内容表面圆角 token 为 `16px`，侧栏圆角 token 为 `24px`；当前侧栏实际使用 `18px`，导航项 `12px`，筛选控件多为 `9px`，普通按钮与详情数据块多为 `8px` 至 `10px`，圆形按钮使用 `50%`。
- 阴影保持克制：亮色为 `0 16px 40px rgb(37 52 77 / 8%)`，暗色为 `0 18px 48px rgb(0 0 0 / 18%)`；主要用于移动侧栏浮层等需要明确前后层级的位置。

## 3. 应用框架

### 3.1 AppLayout

- 宽屏采用 `280px + 自适应内容区` 的双栏网格；侧栏折叠后为 `76px + 自适应内容区`。
- 内容区最大宽度为 `1440px`，居中显示并允许内部模块安全收缩。
- `AppTopbar` 位于内容区上方，路由页面位于其后；菜单展开状态由布局统一维护。
- 首次进入不超过 `620px` 的视口时菜单默认收起；视口切换到该范围时也会自动收起。

### 3.2 AppSidebar

- 侧栏在宽屏中为粘性整高导航，包含品牌、工作区导航与底部账号入口。
- 当前路由同时使用选中底色和左侧香槟金内描边，确保选中关系不依赖单一文字颜色。
- 折叠态保留图标并通过 `title` 提供名称；在紧凑视口隐藏品牌和说明文字。
- 窄屏展开时变为固定浮层，宽 `72px`、距视口边缘 `8px`，并使用阴影建立层级；收起时完全隐藏，避免挤压主内容。

### 3.3 AppTopbar

- 顶栏最小高度为 `64px`，左侧提供菜单开关和“数据投资工作台”身份，右侧提供 `CN-S 工作台` 标识与亮暗主题切换。
- 顶栏菜单与主题按钮均为 `44px × 44px` 的圆形控件，并提供动态 `aria-label`；窄屏隐藏市场标识文字以保留核心操作空间。

## 4. 三档响应式

### 4.1 完整驾驶舱：大于 860px

- 应用保留完整侧栏；J金股筛选、数据状态、六项总览指标和研究模块按桌面信息密度呈现。
- J金股主区域在空间允许时采用“机会雷达 + 金股异动”的主次双栏，次级区域采用“行业共识 + 券商能力 + 市场扩散”三栏；在不超过 `1100px` 时这些模块已按单栏堆叠，以避免内容拥挤。
- 机会雷达使用完整数据表格，并在表体超过 `660px` 时内部纵向滚动。

### 4.2 紧凑布局：620px 至 860px

- 应用壳层在不超过 `840px` 时将导航收为 `68px` 图标栏，缩小页面边距和栏间距。
- J金股在不超过 `860px` 时让标题区、质量说明和筛选栏换行；总览指标改为三列。
- 研究主次区域保持单列堆叠，来源、时间和质量说明允许独占一行，避免被压缩成不可读文本。

### 4.3 窄屏布局：小于 620px

- 主内容改为单栏；侧栏默认隐藏，按需以固定浮层展开，不占据内容宽度。
- J金股标题和更新时间上下排列；筛选区采用两列操作区，月份、券商、行业输入各自跨满整行。
- 六项总览指标改为两列卡片，不再依赖横向滚动。
- 机会雷达隐藏视觉表头，将每条记录转换为两列信息卡；每个值通过 `data-label` 显示字段名，股票与研究状态跨满整行。表格语义仍被保留。
- 顶栏隐藏非关键的市场标识，保留菜单与主题切换。

## 5. 可访问性与交互约定

- 全局键盘焦点使用 `3px solid var(--lk-focus)`，外偏移 `3px`；机会雷达行使用等价的内收焦点环，避免滚动容器裁切。
- 已落地的主要触控控件按至少 `44px` 高或 `44px × 44px` 设置，包括菜单、主题、筛选操作、指标定义、分页、详情关闭和加入自选控件。
- 导航、详情、分页、指标组和市场扩散图提供语义化标签；机会雷达行支持 `Enter` 打开详情。
- 市场扩散柱形区域通过 `role="img"` 和完整月份/数值 `aria-label` 提供文本替代，不要求用户仅凭柱形高度或颜色理解结果。
- 数据完整、部分覆盖、延迟、样本不足、不可比、未分类、研究状态等均直接显示文字；警告色和状态底色只作为辅助。
- 动效遵循 `prefers-reduced-motion: reduce`：应用栏宽动画和侧栏文字淡入淡出会被关闭，J金股页面不会强制平滑滚动。

## 6. J金股驾驶舱结构

驾驶舱按从“范围与可信度”到“研究对象与依据”的顺序组织：

1. 顶部命令区：标题、副标题、数据完整度和生成时间。
2. 统一筛选栏：数据月份、券商、行业，以及应用、清除、刷新操作；刷新期间禁用重复操作。
3. 数据质量行：展示质量说明、数据来源与“月度推荐 + 日线行情”的数据粒度。
4. 总览指标：本月金股、新晋金股、高共识金股、推荐升温、金股突破、20 日平均超额；每项都有可聚焦的定义入口。
5. 主研究区：机会雷达综合排名与金股异动。雷达支持排序、分页、键盘选择，并明确“评分用于研究优先级，不代表收益预测”；异动展示对象、摘要、比较区间、触发规则、数据时间和质量。
6. 次级研究区：行业共识温度、券商选股能力、市场推荐扩散。行业模块分别表达推荐记录数、股票数和券商数；券商模块显示周期、最低样本量、基准和价格口径；扩散模块同时展示数值、月份和环比文字。
7. 股票研究详情：右侧抽屉展示本月推荐券商及记录更新时间、评分组成、历史推荐趋势、行业、研究状态、后复权价格口径、来源、最新行情时间、生成时间和数据质量，并允许加入自选。加入自选后的反馈明确说明不会产生交易行为。

页面已有整体加载、整体错误重试、空模块说明和数据质量降级表达。模块使用共享的表面、边界和文字 token，亮暗主题无需为单个模块维护独立配色。

## 7. 测试夹具水印约定

J金股视觉验收截图使用路由拦截生成的测试夹具数据。截图前，端到端测试会在页面右下角动态插入固定标记“测试夹具数据，仅用于界面验收”，用于避免把示例股票、券商、行业、指标和状态误解为真实业务数据。

该标记只存在于 Playwright 截图流程，不属于生产页面组件。当前验收流程分别生成桌面、窄屏和暗色截图；水印在切换视口和主题时继续保留。
